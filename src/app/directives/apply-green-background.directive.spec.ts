import { ApplyGreenBackgroundDirective } from './apply-green-background.directive';

describe('ApplyGreenBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyGreenBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
